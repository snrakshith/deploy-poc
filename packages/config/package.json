{
  "name": "@sn/config",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "lib": "echo config.....",
    "start": "turbo run start --parallel",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "prettier": "latest"
  }
}
